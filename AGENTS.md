# AGENTS

This repository contains Codex CLI configuration, skills, and agents.

## Skills

Skills live under `skills/` and are invoked automatically based on metadata or manually via
`/skill-name`.

## Agents

Agents live under `agents/` and can be invoked by name when needed.

## Templates

Use `CODEX.md.template` to create project-level context in your repos.
