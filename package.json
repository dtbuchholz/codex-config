{
  "name": "codex-config",
  "version": "1.0.0",
  "description": "Shared Codex CLI configuration",
  "private": true,
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.0",
    "lint-staged": "^16.0.0",
    "prettier": "^3.4.2"
  }
}
